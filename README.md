# WHT-Accelerated Hybrid Correlation Attack

Novel attack on LFSR-based stream ciphers using Walsh-Hadamard Transform
for O(L × 2^L) correlation computation vs O(N × 2^L) for standard attacks.

## Files
- `wht_attack.ipynb` — Main notebook: WHT hybrid attack vs correlation attack comparison
